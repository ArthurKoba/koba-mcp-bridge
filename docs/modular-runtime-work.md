# Modular runtime work

Work in progress on private backend MCP runtimes.

Current branch goals:

- dedicated GitHub runtime;
- dedicated GitLab runtime;
- dedicated Files runtime;
- dedicated HTTP runtime;
- full Files terminology and SQLite migration;
- keep raw Ghidra unchanged;
- retain the current Ghidra adapter only as a transitional gateway layer until a separate
  analysis/recovery MCP is designed.

This branch must not be merged or deployed before explicit approval.
